name: Breaking News Monitor

on:
  schedule:
    # 每 15 分钟检查一次
    - cron: '*/15 * * * *'
  workflow_dispatch:  # 手动触发

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Cache dedup database
        uses: actions/cache@v4
        with:
          path: news_dedup.json
          key: dedup-${{ github.run_id }}
          restore-keys: dedup-

      - name: Run breaking news monitor
        env:
          WECOM_CORP_ID: ${{ secrets.WECOM_CORP_ID }}
          WECOM_CORP_SECRET: ${{ secrets.WECOM_CORP_SECRET }}
          WECOM_AGENT_ID: ${{ secrets.WECOM_AGENT_ID }}
          SILICONFLOW_API_KEY: ${{ secrets.SILICONFLOW_API_KEY }}
          TZ: Asia/Shanghai
        run: python main.py breaking
